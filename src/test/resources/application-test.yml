spring:
  config:
    activate:
      on-profile: test
  jpa:
    properties:
      hibernate:
        show_sql: false
        default_schema: odm_registry_test
        dialect: org.hibernate.dialect.PostgreSQLDialect