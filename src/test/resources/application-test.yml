spring:
  config:
    activate:
      on-profile: test
  jpa:
    hibernate:
      show_sql: false
      default_schema: odm_registry_test
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect